using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DeviceManager : MonoBehaviour {

	// Use this for initialization
	void Start () {

        /*   
         provavelmente ainda vou precisar desta classe mas por agora nao e preciso.
         para mudar a auto rotation do device, vamos ao player settings em File -> Build Settings... -> Player Settings       
         */

    }

    // Update is called once per frame
    void Update () {
        
    }
}
